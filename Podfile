# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'hashTagTest' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'Firebase'
  pod 'Firebase/Firestore'
  pod 'ActiveLabel'

  # Pods for hashTagTest

  target 'hashTagTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'hashTagTestUITests' do
    # Pods for testing
  end

end
